#!/usr/bin/node

class Rectangle {
  constructor (w, h) {
    if (w <= 0 || h <= 0) {}
    else {
      this.width = w;
      this.height = h;
    }
  }
  
  print () {
    for (let i = 0; i < height; i++) {
      console.log('X'.repeat());
    }
  }
}

module.exports = Rectangle;
