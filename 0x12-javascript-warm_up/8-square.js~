#!/usr/bin/node

const process = require('process');
const args = process.argv

const width = parseInt(args[2]);

if (!width) {
  console.log('Missing size');
} else {
    for (let i = 0; i < width; i++) {
      console.log(_.repeat('X', width));
    }
}
